---
sourceFile: "10 Gradient-Based Learning Algorithms – Foundations of Computer Vision"
exportedBy: "Kortex"
exportDate: "2026-05-17T22:54:33.769Z"
---

# 10 Gradient-Based Learning Algorithms – Foundations of Computer Vision

6cb6bded-7e77-4921-8458-33e4ddd3a4cf

10 Gradient-Based Learning Algorithms – Foundations of Computer Vision

66f0d507-9c93-42ff-b380-7e2734e5a057

https://visionbook.mit.edu/gradient\_descent.html

10 Gradient-Based Learning Algorithms – Foundations of Computer Vision

Foundations of Learning

https://visionbook.mit.edu/part\_foundation\_learning.html

10 Gradient-Based Learning Algorithms

https://visionbook.mit.edu/gradient\_descent.html

Foundations of Computer Vision

https://visionbook.mit.edu/

https://github.com/Foundations-of-Computer-Vision/visionbook

https://visionbook.mit.edu/gradient\_descent.html

https://twitter.com/intent/tweet?url=https://visionbook.mit.edu/gradient\_descent.html

https://www.linkedin.com/sharing/share-offsite/?url=https://visionbook.mit.edu/gradient\_descent.html

https://visionbook.mit.edu/gradient\_descent.html

https://visionbook.mit.edu/

https://visionbook.mit.edu/copyright.html

https://visionbook.mit.edu/notations.html

1 The Challenge of Vision

https://visionbook.mit.edu/taxonomy.html

Foundations

https://visionbook.mit.edu/part\_foundations.html

2 A Simple Vision System

https://visionbook.mit.edu/simplesystem.html

3 Looking at Images

https://visionbook.mit.edu/visionscience.html

4 Computer Vision and Society

https://visionbook.mit.edu/fairness.html

Image Formation

https://visionbook.mit.edu/part\_image\_formation.html

https://visionbook.mit.edu/imaging.html

https://visionbook.mit.edu/lenses.html

7 Cameras as Linear Systems

https://visionbook.mit.edu/camera\_as\_linsys.html

https://visionbook.mit.edu/color.html

Foundations of Learning

https://visionbook.mit.edu/part\_foundation\_learning.html

9 Introduction to Learning

https://visionbook.mit.edu/intro\_to\_learning.html

10 Gradient-Based Learning Algorithms

https://visionbook.mit.edu/gradient\_descent.html

11 The Problem of Generalization

https://visionbook.mit.edu/problem\_of\_generalization.html

12 Neural Networks

https://visionbook.mit.edu/neural\_nets.html

13 Neural Networks as Distribution Transformers

https://visionbook.mit.edu/neural\_nets\_as\_distribution\_transformers.html

14 Backpropagation

https://visionbook.mit.edu/backpropagation.html

Foundations of Image Processing

https://visionbook.mit.edu/part\_foundation\_image\_processing.html

15 Linear Image Filtering

https://visionbook.mit.edu/linear\_image\_filtering.html

16 Fourier Analysis

https://visionbook.mit.edu/image\_processing\_fourier.html

Linear Filters

https://visionbook.mit.edu/part\_linear\_filters.html

17 Blur Filters

https://visionbook.mit.edu/blurring\_2.html

18 Image Derivatives

https://visionbook.mit.edu/derivatives.html

19 Temporal filters

https://visionbook.mit.edu/temporal\_filters\_v2.html

Sampling and Multiscale Image Representations

https://visionbook.mit.edu/part\_sampling\_and\_multiscale.html

20 Image Sampling and Aliasing

https://visionbook.mit.edu/sampling\_and\_aliasing.html

21 Downsampling and Upsampling Images

https://visionbook.mit.edu/upsamplig\_downsampling\_2.html

22 Filter Banks

https://visionbook.mit.edu/spatial\_filter\_sets.html

23 Image Pyramids

https://visionbook.mit.edu/pyramids\_new\_notation.html

Neural Architectures for Vision

https://visionbook.mit.edu/part\_neural\_architectures.html

24 Convolutional Neural Nets

https://visionbook.mit.edu/convolutional\_neural\_nets.html

25 Recurrent Neural Nets

https://visionbook.mit.edu/recurrent\_neural\_nets.html

26 Transformers

https://visionbook.mit.edu/transformers.html

Probabilistic Models of Images

https://visionbook.mit.edu/part\_statistical\_image\_models.html

27 Statistical Image Models

https://visionbook.mit.edu/stat\_image\_models\_revised.html

28 Textures

https://visionbook.mit.edu/textures.html

29 Probabilistic Graphical Models

https://visionbook.mit.edu/graphical\_models.html

Generative Image Models and Representation Learning

https://visionbook.mit.edu/part\_generative\_models\_and\_representation.html

30 Representation Learning

https://visionbook.mit.edu/representation\_learning.html

31 Perceptual Grouping

https://visionbook.mit.edu/perceptual\_organization.html

32 Generative Models

https://visionbook.mit.edu/generative\_models.html

33 Generative Modeling Meets Representation Learning

https://visionbook.mit.edu/generative\_modeling\_and\_rep\_learning.html

34 Conditional Generative Models

https://visionbook.mit.edu/conditional\_generative\_models.html

Challenges in Learning-Based Vision

https://visionbook.mit.edu/part\_challenges\_in\_learning\_based\_vision.html

35 Data Bias and Shift

https://visionbook.mit.edu/bias\_and\_shift.html

36 Training for Robustness and Generality

https://visionbook.mit.edu/data\_augmentation.html

37 Transfer Learning and Adaptation

https://visionbook.mit.edu/transfer\_learning.html

Understanding Geometry

https://visionbook.mit.edu/part\_understanding\_geometry.html

38 Representing Images and Geometry

https://visionbook.mit.edu/homogeneous\_coordinates.html

39 Camera Modeling and Calibration

https://visionbook.mit.edu/imaging\_geometry.html

40 Stereo Vision

https://visionbook.mit.edu/3d\_scene\_understanding\_stereo.html

41 Homographies

https://visionbook.mit.edu/homography.html

42 Single View Metrology

https://visionbook.mit.edu/3d\_scene\_understanding\_single\_view.html

43 Learning to Estimate Depth from a Single Image

https://visionbook.mit.edu/3d\_learning.html

44 Multiview Geometry and Structure from Motion

https://visionbook.mit.edu/multiview.html

45 Radiance Fields

https://visionbook.mit.edu/nerf.html

Understanding Motion

https://visionbook.mit.edu/part\_understanding\_motion.html

46 Motion Estimation

https://visionbook.mit.edu/motion\_estimation\_intro.html

47 3D Motion and Its 2D Projection

https://visionbook.mit.edu/2d\_motion\_from\_3d.html

48 Optical Flow Estimation

https://visionbook.mit.edu/optical\_flow.html

49 Learning to Estimate Motion

https://visionbook.mit.edu/motion\_estimation.html

Understanding Vision with Language

https://visionbook.mit.edu/part\_scene\_understanding.html

50 Object Recognition

https://visionbook.mit.edu/object\_recognition\_v3.html

51 Vision and Language

https://visionbook.mit.edu/VLMs.html

On Research, Writing and Speaking

https://visionbook.mit.edu/part\_on\_research.html

52 How to Do Research

https://visionbook.mit.edu/how\_to\_do\_research.html

53 How to Write Papers

https://visionbook.mit.edu/how\_to\_write\_papers.html

54 How to Give Talks

https://visionbook.mit.edu/how\_to\_give\_talks.html

Challenges in Learning-Based Vision

https://visionbook.mit.edu/part\_closing\_remarks.html

55 A Simple Vision System—Revisited

https://visionbook.mit.edu/simplesystem\_final.html

https://visionbook.mit.edu/references.html

Adaptive Computation and Machine Learning Series

https://visionbook.mit.edu/series.html

#### Table of contents

10.1 Introduction

https://visionbook.mit.edu/gradient\_descent.html#introduction

10.2 Technical Setting

https://visionbook.mit.edu/gradient\_descent.html#technical-setting

10.3 Basic Gradient Descent

https://visionbook.mit.edu/gradient\_descent.html#basic-gradient-descent

10.4 Learning Rate Schedules

https://visionbook.mit.edu/gradient\_descent.html#learning-rate-schedules

10.5 Momentum

https://visionbook.mit.edu/gradient\_descent.html#momentum

10.6 What Kinds of Functions Can Be Minimized with Gradient Descent?

https://visionbook.mit.edu/gradient\_descent.html#what-kinds-of-functions-can-be-minimized-with-gradient-descent

10.6.1 Gradient-Like Optimization for Functions without Good Gradients

https://visionbook.mit.edu/gradient\_descent.html#sec-gradient\_descent-zeroth\_order

10.6.2 Gradient Clipping

https://visionbook.mit.edu/gradient\_descent.html#gradient-clipping

10.7 Stochastic Gradient Descent

https://visionbook.mit.edu/gradient\_descent.html#sec-gradient\_descent-SGD

10.8 Concluding Remarks

https://visionbook.mit.edu/gradient\_descent.html#concluding-remarks

Edit this page

https://github.com/Foundations-of-Computer-Vision/visionbook/edit/main/gradient\_descent.qmd

Report an issue

https://github.com/Foundations-of-Computer-Vision/visionbook/issues/new

Other Links

Accessibility

https://accessibility.mit.edu/

Foundations of Learning

https://visionbook.mit.edu/part\_foundation\_learning.html

10 Gradient-Based Learning Algorithms

https://visionbook.mit.edu/gradient\_descent.html

10 Gradient-Based Learning Algorithms

10.1 Introduction

Once you have specified a learning problem (loss function, hypothesis space, parameterization), the next step is to find the parameters that minimize the loss. This is an optimization problem, and the most common optimization algorithm we will use is

gradient descent

. Gradient descent is like a skier making their way down a snowy mountain, where the shape of the mountain is the loss function.

There are many varieties of gradient descent, and we will call this whole family

gradient-based learning algorithms

. All share the same basic idea: at some operating point, calculate the direction of steepest descent, then use this direction to find a new operating point with lower loss.

We use the term

operating point

to refer to a particular point (setting of the parameters) where we are currently evaluating the loss.

10.2 Technical Setting

In this chapter, we consider the task of minimizing a cost function J : ⋅ → R , which is a function that maps some arbitrary input to a scalar cost.

In learning problems, the domain of J is the training data and the parameters θ . We will often consider the training data to be fixed and only denote the objective as a function of the parameters, J ( θ ) . Our goal is to solve: θ ∗ = arg  min θ J ( θ )

Pretty much all optimizers work by some iterative process, where they update the parameters to be better and better. Different optimizers differ in how the parameter update function works. The update function gets to view some information about the loss landscape, then uses that information to update the parameters, as shown in

Figure 10.1

https://visionbook.mit.edu/gradient\_descent.html#fig-gradient\_descent-optimization\_schematic

Figure 10.1: General optimization loop.

In the simplest setting, called

zeroth-order optimization

, the update function only gets to observe the value J ( θ ) . The only way, then, to find θ 's that minimize the loss is to sample different values for θ and move toward the values that are lower.

gradient-based optimization

, also called

first-order optimization

, the update function takes as input the gradient of the cost with respect to the parameters at the current operating point, ∇ θ J ( θ ) . This reveals hugely useful information about the loss that directly tells us how to minimize it: just move in the direction of steepest descent, that is, the gradient direction.

Higher-order optimization methods observe higher-order derivatives of the loss, such as the Hessian H , which tells you how the landscape is locally curving. The Hessian is costly to compute, but many methods use approximations to the Hessian, or other properties related to loss curvature, and these are growing in popularity~.

10.3 Basic Gradient Descent

The simplest version of gradient descent just takes a step in the gradient direction of length proportional to the gradient magnitude. This algorithm is described in

Algorithm 10.1

https://visionbook.mit.edu/gradient\_descent.html#alg-gradient\_descent\_basic\_gradient\_descent

Algorithm 10.1: Gradient descent

. Optimizing a cost function J : θ → R by descending the gradient ∇ θ J .

This algorithm has two hyperparameters, the

learning rate

η , which controls the step size (learning rate times gradient magnitude), and the number of steps K . If the learning rate is sufficiently small and the initial parameter vector θ 0 is random, then this algorithm will almost surely converge to a local minimum of J as K → ∞ ~. However, to descend more quickly, it can be useful to set the learning rate to a higher value.

10.4 Learning Rate Schedules

A generally useful strategy is to start with a high value for η and then decay it until convergence according to a

learning rate schedule

. Researchers have come up with innumerable schedules, and they generally work by calling some function lr ( η 0 , k ) to get the learning rate on each iteration of descent: η k = lr ( η 0 , k ) Generally, we want an update rule where η k + 1 < η k , so that we take smaller steps as we approach the minimizer. A few simple and popular approaches are given below:

lr ( η 0 , k ) = β − k η 0 ◃ exponential decay lr ( η 0 , k ) = β − ⌊ k / M ⌋ η 0 ◃ stepwise exponential decay lr ( η 0 , k ) = ( K − k ) K η 0 ◃ linear decay

One downside of linear decay is that it depends on the total number of steps K . This makes it hard to compare optimization runs of different lengths. This is something to also be aware of in more advanced learning rate schedules, such as cosine decay~, which also have different behavior for different settings of K .

The β and M are additional hyperparameters of these methods. The general approach of learning rate decay is summarized in

Algorithm 10.2

https://visionbook.mit.edu/gradient\_descent.html#alg-gradient\_descent\_gradient\_descent\_with\_lr\_decay

Algorithm 10.2: Gradient descent with learning rate decay algorithm.

Variations on this algorithm include only decaying the learning rate when a plateau is reached (i.e., when the loss is not decreasing for many iterations in a row), or decaying the learning rate according to more complex nonlinear schedules, such as one shaped like a cosine function~.

10.5 Momentum

Could we do a smarter update than just taking a step in the direction of the gradient? Of the countless ideas that have been proposed, one of the few that has stuck is

~. Momentum makes the analogy to skiing even more precise: momentum is like the inertia of the skier, carrying them over the little bumps and imperfections in the ski slope and increasing their speed as they descend along a straight path. In math, momentum just means that we set the parameter update to be a direction v k + 1 , given by a weighted combination of the previous update direction, v k , plus the current negative gradient: v k + 1 = μ v k − η ∇ θ J ( θ k ) The weight μ in this combination is a new hyperparameter, sometimes simply called the momentum. The full algorithm is given in

Algorithm 10.3

https://visionbook.mit.edu/gradient\_descent.html#alg-gradient\_descent\_gradient\_descent\_with\_momentum

Algorithm 10.3: Gradient descent with momentum algorithm.

Figure 10.2

https://visionbook.mit.edu/gradient\_descent.html#fig-gradient\_descent-momentum\_out1

shows how momentum affects gradient descent for a simple objective J = abs ( θ ) (absolute value of θ ). As can be seen in the figure, some momentum can help the convergence rate ( μ = 0.5 ) but too much momentum will cause the trajectory to overshoot the optimum and even when the optimum loss is achieved, the trajectory might not stop (

Figure 10.2

https://visionbook.mit.edu/gradient\_descent.html#fig-gradient\_descent-momentum\_out1

μ = 0.95 ).

Figure 10.2: (left) A simple loss function J = abs ( θ ) . (right) Optimization trajectory for three different settings of momentum μ . White line indicates value of the parameter at each iteration of optimization, starting at top and progressing to bottom. Color is value of the loss. Red dot is location where loss first reaches within 0.01 of optimal value.

It is also possible to come up with other kinds of momentum, which bias the update direction based on some accumulated information from previous updates. Two popular alternatives, which you can read up on elsewhere, are Nesterov's accelerated gradient~ and Adam~.

10.6 What Kinds of Functions Can Be Minimized with Gradient Descent?

What if a function is not differentiable? Can we still use gradient descent? Sometimes! The property we need is that we can get a meaningful signal as to how to perturb the function's parameters in order to reduce the loss. This property is

the same as differentiability defined in math textbooks. A function may be differentiable but not give useful gradients (e.g., if the gradient is zero everywhere), and a function may be nondifferentiable (at certain points) but still allow for meaningful gradient-based updates (e.g., ).

Figure 10.3

https://visionbook.mit.edu/gradient\_descent.html#fig-gradient\_descent-grad\_descent\_simple\_examples

gives examples of different types of functions being minimized with gradient descent.

Figure 10.3

https://visionbook.mit.edu/gradient\_descent.html#fig-gradient\_descent-grad\_descent\_simple\_examples

Figure 10.3

https://visionbook.mit.edu/gradient\_descent.html#fig-gradient\_descent-grad\_descent\_simple\_examples

(d) are cases where the function is discontinuous, and the analytical derivative is undefined at the discontinuity. Surprisingly, in

Figure 10.3

https://visionbook.mit.edu/gradient\_descent.html#fig-gradient\_descent-grad\_descent\_simple\_examples

(b), this is not a problem for gradient descent. This is because the gradient descent algorithm we are using here (the one used by Pytorch~) uses a

one-sided derivative

at the discontinuity, that is, we set the gradient at the discontinuity to be equal to the gradient value an infinitesimal step away from the discontinuity in a fixed arbitrary direction. Under the hood, for each atomic discontinuous operation, Pytorch requires that we define its gradients at the discontinuities, and the one-sided gradient is a standard choice. This is why it can be fine in deep learning to use functions like rectified nonlinear units (ReLU), which are common in deep networks and have discontinuous gradients.

Figure 10.3

https://visionbook.mit.edu/gradient\_descent.html#fig-gradient\_descent-grad\_descent\_simple\_examples

(c) and (e) give cases where the function is continuous, but the gradients are not well-behaved. In

Figure 10.3

https://visionbook.mit.edu/gradient\_descent.html#fig-gradient\_descent-grad\_descent\_simple\_examples

(c) we have a gradient that has nearly

, that is, it is near zero everywhere, and gradient descent, with a fixed learning rate, will therefore be slow.

Figure 10.3

https://visionbook.mit.edu/gradient\_descent.html#fig-gradient\_descent-grad\_descent\_simple\_examples

(e) shows the opposite scenario: the gradient at the minimizer goes to infinity; we call this an

exploding gradient

, and this leads to failures of convergence.

Figure 10.3

https://visionbook.mit.edu/gradient\_descent.html#fig-gradient\_descent-grad\_descent\_simple\_examples

(f) shows one more problematic case: when there are multiple minima, gradient descent can get stuck in a suboptimal minimum. Which minimum we arrive at will depend on where we initialized x .

Figure 10.3: How gradient descent behaves on various functions.\*\* In each subplot, the left shows the function J , with the red point representing the solution found by gradient descent (GD) with η = 0.01 and μ = 0.9 . The right shows the trajectory of x values over iterations of GD, plotted on top of J at each iteration. (a) As η goes to zero, GD converges for convex functions. (b) Discontinuities pose no essential problem, as long as the gradient is defined on either side. (c) A nearly flat function will exhibit very slow descent. (d) Piecewise constant functions are problematic because the gradient completely vanishes. (e) For the function J = sqrt ( abs ( θ ) ) − 0.25 , the gradient goes to infinity at the minimizer, causing instability. (f) When J has multiple local minima, we may not find the global minimum.

10.6.1 Gradient-Like Optimization for Functions without Good Gradients

What about minimizing functions like

Figure 10.3

https://visionbook.mit.edu/gradient\_descent.html#fig-gradient\_descent-grad\_descent\_simple\_examples

(d), where the gradient is zero almost everywhere? This is a case where gradient descent truly struggles. However, it is often possible to transform such a problem into one that can be treated with gradient descent. Remember that the key property of a gradient, from the perspective of optimization, is that it is a locally loss-minimizing direction in parameter space. Most gradient-based optimizers don't really need true gradients; instead, their update functions are compatible with a broader family of local loss-minimizing directions, v .

Besides the true gradient, what are some other good choices for v ? One common idea is to set v to be the gradient of a

surrogate loss

function, which is a function, J surr , with meaningful (non-zero) gradients that approximates J . An example might be a smoothed version of J . Another way to get v is to compute it by sampling perturbations of θ , and seeing which perturbation leads to lower loss. In this strategy, we evaluate J ( θ + ϵ ) for a set of perturbations ϵ , then move toward the ϵ 's that decreased the loss. Approaches of this kind are sometimes called

evolution strategies

~, and a basic version of this algorithm is given in

Algorithm 10.4

https://visionbook.mit.edu/gradient\_descent.html#alg-gradient\_descent\_ES

Algorithm 10.4: Evolution strategy algorithm.

As shown in

Figure 10.4

https://visionbook.mit.edu/gradient\_descent.html#fig-gradient\_descent-sampling\_out1

, this algorithm can successfully minimize the function in

Figure 10.3

https://visionbook.mit.edu/gradient\_descent.html#fig-gradient\_descent-grad\_descent\_simple\_examples

Figure 10.4: Using

Algorithm 10.4

https://visionbook.mit.edu/gradient\_descent.html#alg-gradient\_descent\_ES

) to minimize a nondifferentiable (zero-gradient) loss, using σ = 1 , M = 10 , and η = 0.02 .

10.6.2 Gradient Clipping

Figure 10.3

https://visionbook.mit.edu/gradient\_descent.html#fig-gradient\_descent-grad\_descent\_simple\_examples

(e), where the gradient explodes near the optimum? Is there anything we can do to improve the optimization of this function? To combat exploding gradients, a useful trick is

gradient clipping

, which just means clamping the magnitude of the gradient to some maximum value.

Algorithm 10.5

https://visionbook.mit.edu/gradient\_descent.html#alg-gradient\_descent\_grad\_clipping

describes this approach.

Algorithm 10.5: Gradient clipping algorithm.

is the “clipping” function: clip ( v , − m , m ) = max ( min ( v , m ) , − m )

This algorithm indeed successfully minimizes our example of the exploding gradient, as can be seen in

Figure 10.5

https://visionbook.mit.edu/gradient\_descent.html#fig-gradient\_descent-clipped\_out1

Figure 10.5: Using

with clipping to minimize a loss with exploding gradients, using m = 0.1 .

10.7 Stochastic Gradient Descent

One problem with the gradient-based methods we have seen so far is that the gradient may in fact be very expensive to compute, and this is often the case for learning problems. This is because learning problems typically have the form that J is the average of losses incurred on each training datapoint. Computing ∇ θ J ( θ ) requires computing the gradient for each element in the average, that is, the gradient of the function being learned evaluated at the location of each datapoint in the training set. If we train on a big dataset, say 1 million training points, then to perform just step of gradient descent requires computing 1 million gradients! To make this clear, we will write out J as an explicit function of the training data { x ( i ) , y ( i ) } i = 1 N . For typical learning problems, ∇ θ J ( θ , { x ( i ) , y ( i ) } i = 1 N ) decomposes as follows: ∇ θ J ( θ , { x ( i ) , y ( i ) } i = 1 N ) = ∇ θ 1 N ∑ i = 1 N L ( f θ ( x ( i ) ) , y ( i ) ) = 1 N ∑ i = 1 N ∇ θ L ( f θ ( x ( i ) ) , y ( i ) )

For large N , computing this sum is very expensive. Suppose instead we randomly subsample (without replacement) a of terms from this sum, { x ( b ) , y ( b ) } b = 1 B , where B is the

. We then compute an of the total gradient as the average gradient over this batch as follows: g ~ = 1 N ∑ b = 1 B ∇ θ L ( f θ ( x ( b ) ) , y ( b ) )

If we sample a large batch, where B is almost as large as N , then the average over the B terms should be roughly the same as the average over all N terms. If we sample a smaller batch, then our estimate of the gradient will be less accurate but faster to compute. Therefore we have a tradeoff between accuracy and speed, and we can navigate this tradeoff with the hyperparameter B .

The variant of gradient descent that uses this idea is called

stochastic gradient descent

(SGD), because each iteration of descent uses a different randomly (stochastically) sampled batch of training data to estimate the gradient.

The full description of SGD is given in

Algorithm 10.6

https://visionbook.mit.edu/gradient\_descent.html#alg-gradient\_descent\_SGD

Algorithm 10.6: Stochastic gradient descent algorithm. Stochastic gradient descent estimates the gradient from a stochastic subset (batch) of the full training data, and makes an update on that basis.

has a number of useful properties beyond just being faster to compute than

. Because each step of descent is somewhat random, SGD can jump over small bumps in the loss landscape, as long those bumps disappear for some randomly sampled batches. Another important property is that SGD can implicitly regularize the learning problem. For example, for linear problems (i.e., f θ is linear), then if there are multiple parameter settings that minimize the loss, SGD will often converge to the solution with minimum parameter norm~.

10.8 Concluding Remarks

The study of optimization can fill dozens of textbooks and thousands of academic papers. But fortunately for us, modern machine learning has converged on just a few very simple optimization methods that are used in practice. We will soon encounter deep learning, which is the main kind of machine learning used for computer vision. In deep learning, gradient-based optimization is the workhorse. Believe it or not, the handful of algorithms described above are enough to train most state-of-the-art deep learning models. Every year there are new elaborations on these ideas, and second-order methods are ever on the horizon, yet the basic concepts remain quite simple: compute a local estimate of the shape of the loss landscape, then, based on this shape, take a small step toward a lower loss.

9 Introduction to Learning

https://visionbook.mit.edu/intro\_to\_learning.html

11 The Problem of Generalization

https://visionbook.mit.edu/problem\_of\_generalization.html

All content licensed under

CC-BY-NC-ND

https://creativecommons.org/licenses/by-nc-nd/4.0/

Antonio Torralba

https://groups.csail.mit.edu/vision/torralbalab/

Phillip Isola

https://web.mit.edu/phillipi/

William Freeman

https://billf.mit.edu/

Edit this page

https://github.com/Foundations-of-Computer-Vision/visionbook/edit/main/gradient\_descent.qmd

Report an issue

https://github.com/Foundations-of-Computer-Vision/visionbook/issues/new

Translation to web format by

https://shenshen.mit.edu/

Contributors

https://github.com/Foundations-of-Computer-Vision/visionbook/graphs/contributors

https://quarto.org/

